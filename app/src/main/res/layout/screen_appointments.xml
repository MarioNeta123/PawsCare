<?xml version="1.0" encoding="utf-8"?>

<!-- Pantalla de citas médicas: reserva, próxima cita y citas anteriores -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cream"
    android:clipChildren="false"
    android:clipToPadding="false"
    tools:ignore="VisualLintSystemUi">

    <!-- Contenido superior desplazable: encabezado, acciones de cita, próxima cita y título de citas anteriores -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/top_scroll"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fillViewport="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Encabezado de la pantalla de citas médicas -->
            <include
                android:id="@+id/appointment_section_header"
                layout="@layout/component_section_header" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingTop="10dp"
                android:paddingBottom="0dp">

                <!-- Par de botones de acción rápida (p. ej. agendar / historial de citas) -->
                <include
                    android:id="@+id/duob_appointment"
                    layout="@layout/item_duo_button" />

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="10dp" />

                <!-- Botón para agregar una nueva cita médica -->
                <include
                    android:id="@+id/add_appointment"
                    layout="@layout/item_icon_button" />

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="10dp" />

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp">

                <!-- Título de la sección de la próxima cita médica -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/appointments_"
                    android:textSize="18sp"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:fontFamily="@font/kanit" />

                <!-- Tarjeta con el tipo de cita (consulta, revisión, etc.) -->
                <include
                    android:id="@+id/type_appointment"
                    layout="@layout/item_default_b" />

                <!-- Tarjeta con la información del doctor asignado a la cita -->
                <include
                    android:id="@+id/doctor"
                    layout="@layout/item_default_a" />

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingTop="10dp">

                <!-- Título de la sección de citas médicas anteriores -->
                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/old_appointments"
                    android:textSize="18sp"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:fontFamily="@font/kanit" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- Área desplazable con el listado de citas médicas anteriores -->
    <ScrollView
        android:id="@+id/old_appointments_feed"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/top_scroll"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation">

        <!-- Contenedor de tarjetas de citas anteriores -->
        <LinearLayout
            android:id="@+id/layout_old_appointments_placeholder"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingStart="24dp"
            android:paddingEnd="24dp">

            <!-- Tarjeta individual de cita médica anterior -->
            <include
                android:id="@+id/old_appointment"
                layout="@layout/item_default_b" />

        </LinearLayout>

    </ScrollView>

    <!-- Barra de navegación inferior de la app -->
    <include
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        layout="@layout/component_bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>