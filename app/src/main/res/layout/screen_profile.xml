<?xml version="1.0" encoding="utf-8"?>

<!-- Pantalla de perfil: datos del usuario, información personal, mascotas y acciones de cuenta -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cream"
    android:clipChildren="false"
    android:clipToPadding="false"
    tools:ignore="VisualLintSystemUi">

    <!-- Contenido desplazable de la pantalla de perfil; se ajusta al espacio disponible sobre la barra inferior -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/top_scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation">

        <!-- Organiza los elementos de forma vertical -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Encabezado de la pantalla de perfil -->
            <include
                android:id="@+id/profile_section_header"
                layout="@layout/component_section_header" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="24dp"
                android:paddingEnd="24dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp">

                <!-- Tarjeta con avatar y nombre del usuario -->
                <include
                    android:id="@+id/user_info_simple"
                    layout="@layout/item_user_simple" />

                <!-- Título de la sección de datos personales -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/personal_data"
                    android:textSize="18sp"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:fontFamily="@font/kanit" />

                <!-- Tarjeta con email, teléfono y dirección del usuario -->
                <include
                    android:id="@+id/user_info"
                    layout="@layout/item_user_info" />

                <!-- Título de la sección de mascotas del usuario -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/personal_pets"
                    android:textSize="18sp"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:fontFamily="@font/kanit" />

                <!-- Contenedor desplazable de las mascotas registradas -->
                <ScrollView
                    android:id="@+id/scroll_styles_holder"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:scrollbars="none">

                    <!-- Contenedor de las tarjetas de mascota -->
                    <LinearLayout
                        android:id="@+id/layout_styles_placeholder"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center">

                        <!-- Tarjeta individual de mascota -->
                        <include
                            android:id="@+id/styles"
                            layout="@layout/item_pet_info_simple" />

                    </LinearLayout>

                </ScrollView>

                <!-- Botón para editar la información del usuario -->
                <include
                    android:id="@+id/edit_user"
                    layout="@layout/item_button"/>

                <Space
                    android:layout_width="match_parent"
                    android:layout_height="10dp"/>

                <!-- Botón para cerrar sesión -->
                <include
                    android:id="@+id/close_session"
                    layout="@layout/item_button"/>

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- Barra de navegación inferior de la app -->
    <include
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:clipChildren="false"
        android:clipToPadding="false"
        layout="@layout/component_bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>